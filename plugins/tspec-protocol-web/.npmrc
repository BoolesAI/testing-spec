# Skip browser downloads during npm install
# Puppeteer will download browsers on-demand when needed
# Note: If this doesn't work, use: PUPPETEER_SKIP_DOWNLOAD=true npm install
puppeteer_skip_chromium_download=true
puppeteer_skip_download=true
